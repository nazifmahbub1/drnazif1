HEADSHOT GOES HERE
==================
Save Dr. Nazif's headshot in THIS folder with EXACTLY this filename:

    dr-nazif-headshot.jpg

The site already points to it (assets/img/dr-nazif-headshot.jpg) on the
Home and About pages. As soon as the file exists, it replaces the
placeholder automatically — no code changes needed.

Recommended: a portrait-orientation photo, roughly 4:5 ratio
(e.g. 1000 x 1250 px or larger), face centred near the top.
A .png also works — if you use PNG, rename references or just keep the
.jpg name on the file.
